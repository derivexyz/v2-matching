// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.0;

contract TSAShareHandler {
  /**
   * User deposits
   * - collect "connector", "recipient" and "fallback" addresses
   * User withdrawals
   * - collect "connector", "recipient" and "fallback" addresses
   * total pending deposits + withdrawals
   * "burnt 1 share, got X tokens"
   */
  constructor() {}
}
